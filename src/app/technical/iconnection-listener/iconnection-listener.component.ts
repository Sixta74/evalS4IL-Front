// import { User } from "../model/user";

// export interface IConnectionListener {
//     /**
//      * This method wars the listener that a change has come in the connection status.
//      * @param newProfile
//      */
//     connectionChanged(newProfile : User | null) : void;
//     /**
//      * @returns the name of current listener as this name must be unique.
//      * best is to une listener's class name.
//      */
//     getName() : string;
// }
